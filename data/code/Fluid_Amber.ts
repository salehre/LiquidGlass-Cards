import type { CardVisualConfig } from './generateFrameworkCode'

/** Fluid Amber — stronger, more "fluid" distortion than Liquid Crystal. */
export const fluidAmberConfig: CardVisualConfig = {
  slug: 'fluid-amber-card',
  filterId: 'glass-distortion-02',
  baseFrequency: 0.018,
  scale: 120,
  blur: 12,
  insetShadow: '0 0 4px -8px',
}
