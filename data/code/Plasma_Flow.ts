import type { CardVisualConfig } from './generateFrameworkCode'

/** Plasma Flow — highest baseFrequency/scale in the set, light blur, sharp warp. */
export const plasmaFlowConfig: CardVisualConfig = {
  slug: 'plasma-flow-card',
  filterId: 'glass-distortion-09',
  baseFrequency: 0.035,
  scale: 180,
  blur: 4,
  insetShadow: '0 0 3px -10px',
}
