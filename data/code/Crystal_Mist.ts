import type { CardVisualConfig } from './generateFrameworkCode'

/** Crystal Mist — sits between Ocean Wave and Fluid Amber in strength. */
export const crystalMistConfig: CardVisualConfig = {
  slug: 'crystal-mist-card',
  filterId: 'glass-distortion-06',
  baseFrequency: 0.02,
  scale: 110,
  blur: 14,
  insetShadow: '0 0 8px -2px',
}
