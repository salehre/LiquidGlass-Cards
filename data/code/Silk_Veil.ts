import type { CardVisualConfig } from './generateFrameworkCode'

/** Silk Veil — lowest-strength distortion, heaviest blur, soft and silky. */
export const silkVeilConfig: CardVisualConfig = {
  slug: 'silk-veil-card',
  filterId: 'glass-distortion-08',
  baseFrequency: 0.006,
  scale: 45,
  blur: 18,
  insetShadow: '0 0 6px -3px',
}
