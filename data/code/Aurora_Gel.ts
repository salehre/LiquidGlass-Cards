import type { CardVisualConfig } from './generateFrameworkCode'

/** Aurora Gel — mid-strength, smooth gel-like wobble. */
export const auroraGelConfig: CardVisualConfig = {
  slug: 'aurora-gel-card',
  filterId: 'glass-distortion-11',
  baseFrequency: 0.022,
  scale: 100,
  blur: 11,
  insetShadow: '0 0 7px -5px',
}
