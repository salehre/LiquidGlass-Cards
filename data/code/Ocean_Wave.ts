import type { CardVisualConfig } from './generateFrameworkCode'

/** Ocean Wave — mid-strength wave between Ice Ripple and Fluid Amber. */
export const oceanWaveConfig: CardVisualConfig = {
  slug: 'ocean-wave-card',
  filterId: 'glass-distortion-05',
  baseFrequency: 0.015,
  scale: 95,
  blur: 10,
  insetShadow: '0 0 4px -8px',
}
