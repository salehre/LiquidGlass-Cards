import type { CardVisualConfig } from './generateFrameworkCode'

/** Molten Glass — highest baseFrequency of the set, a hot roiling distortion. */
export const moltenGlassConfig: CardVisualConfig = {
  slug: 'molten-glass-card',
  filterId: 'glass-distortion-07',
  baseFrequency: 0.03,
  scale: 140,
  blur: 8,
  insetShadow: '0 0 10px -6px',
}
